import { HStack, Image, Text } from "@chakra-ui/react";

const NavBar = () => {
  return (
    <HStack>
      <Image src="" alt="logo" sizes="60" />
      <Text>NavBar</Text>
    </HStack>
  );
};

export default NavBar;
